import {AvatarSelection} from "./comps/avatar_selection.tsx"
import {DashboardLayout} from "@/components/utils_for_components/dashboard_layout.tsx";

export default function AvatarPage() {

    return (
        <DashboardLayout>
            <AvatarSelection/>
        </DashboardLayout>
    )
}

